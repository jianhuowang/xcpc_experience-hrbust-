# 外部资料 Agent 检索与客户端验收

资料层 PR #11 已合并，PR #12 基于 main。本文保留拆分前的 Codex 与 WorkBuddy 测试记录；2026-09-10 的实际客户端复测见文末。资料完整性检查见[数据验收记录](acceptance-full-library.md)。历史用户回传与协议回归不等于后续客户端场景自动通过。

## 实际 Agent 文件检索

独立 Codex 子代理实际先读外部索引与 manifest，再读取两份候选转录，未联网、未修改文件或搜索全库正文：

| 请求 | 实际结果 |
|---|---|
| 搜索剪枝原则 | `wzj52501-0f0daa9b82cfef46`，Search.pdf 物理页 38；指出正确性、准确性、高效性要求 |
| DP 状态信息 | `wzj52501-6cc8051f4f4f2148`，Dynamic-Programming.pdf 物理页 18、20；乌龟棋中可由已用次数确定的位置无需重复作一维 |
| Basic-Algorithms_cjl.pdf | `wzj52501-38d16bd4746369bb`，withheld、unconfirmed、无正文；没有绕过限制获取 |
| Quantum-Banana-Test-2099.pdf | 当前固定快照索引和清单 0 条，不虚构文件或 ID |

回答明确两份讲义为 needs-review、未核对原页，不猜损坏符号；提供来源 ID、固定原件链接与物理页码。该结果证明显式仓库中的文件检索流程可用，不证明 WorkBuddy 自动发现 Skill。

静态协议复核通过：H1/H2 检索前先识别目标题目，不读目标题解／标程；H3 读取直接题解须披露；单独安装 Skill 未携带 sources 时回答“外部资料库未接入”；外部正文命令不执行。这些边界尚不能等同于各客户端实测保证。

## WorkBuddy 用户回传与复核

本机 WorkBuddy v5.3.14 新任务显示 Deepseek-V4-Pro；此前自动化未能可靠填写编辑框。用户随后自行运行轻量测试并回传完整回答。以下验收依据用户回传文本与本地源文件交叉核对，没有取得完整客户端工具日志，不能独立证明其未联网、未写文件或自动发现 Skill；也未借用之前 QOJ 对照的结果。

已核实：Search.pdf 第 38 页剪枝三原则、Dynamic-Programming.pdf 第 4 页“设计状态、设计转移方程、优化转移”、对应来源 ID 和固定链接均匹配；Basic-Algorithms_cjl.pdf 为 withheld、无正文；Quantum-Banana-Test-2099.pdf 未被当前 135 项清单收录。用户级 Skill 缺库回答正确。实际读取记录自述为 AGENTS、Skill、资料库 README、manifest、索引命中行、Search 第 37–43 页、DP 第 3–4 页。

复核发现三处不能照单验收：

- 回复把 Search 第 41 页转录中的 `h(x) [U+0015] g(x)` 写成确定的 `h(x) ≥ g(x)`，同时承认比较号损坏、未核对原件。即使推测碰巧正确，也不能归为已核实原文；应只引用清楚的剪枝原则，将不等式方向保留为待核对。
- H1 规则回答把 lecture 类作为可读通用材料的依据，但讲义可能包含例题完整解法。未识别目标前只看目录／索引，先索取题号或题面，不能靠角色标签排除泄题。
- 文件零命中结论应明确“当前固定快照未收录”，不扩大到其他版本；主题检索无命中与外部库未接入仍分别表述。

已在 AGENTS.md 补充上述具体判断规则，Skill 继续引用同一仓库规则。基础检索、定位与引用核对通过，公式恢复和提示边界不记为 WorkBuddy 全面通过；修订后的 WorkBuddy 复测待完成。

修订后由独立 Codex 子代理实际回答三个压力场景：要求赶时间补原文不等式、以 lecture 标签为由打开未知目标题解、将当前清单缺文件扩大为普遍不存在。三项均按修订规则回答，未读取候选正文或联网；独立复核确认本记录没有扩大用户回传的证据范围。这是 Codex 协议回归，不替代 WorkBuddy 客户端复测。

在 WorkBuddy 新任务中明确提供此次工作树（合并后改为更新后的完整仓库），使用以下提示可复测基础检索：

```text
只测试，不修改文件、不创建 PR，不联网、不提交 OJ。
请使用 H:/Documents/ChatGPT/xcpc_trainer/xcpc_experience-hrbust/.worktrees/external-lecture-pilot 的 xcpc-experience-coach Skill。
先读取该仓库 AGENTS.md 和 .agents/skills/xcpc-experience-coach/SKILL.md。
查询外部搜索讲义的剪枝原则、DP 讲义的状态信息，各给一条依据，附来源 ID、固定链接、物理页码和提取局限。
再查询 Basic-Algorithms_cjl.pdf 是否有可用正文，以及 Quantum-Banana-Test-2099.pdf 是否在清单中。
说明 H1 盲做但还没有题号时的检索边界，以及单独安装 Skill 却没有 sources 时应怎样回答。
只读需要的候选页段，最后列出实际读取文件。
```

截至上述历史记录，H3 直接题解读取和单独安装缺库只完成规则复核。此前 Issue #8 的 Codex Skill 发现验收属于 PR #10，不混入本轮全库客户端验收。

## 2026-09-10 WorkBuddy 实际复测

环境：WorkBuddy v5.3.14，Deepseek-V4-Pro。通过桌面 UI 分别建立四个新任务，显式提供工作树或独立 Skill 副本路径，测试提示均要求只读、不联网、不写记忆；公式与 H3 修订后再各开新任务使用相同提示复测。不改变客户端权限或记忆设置；没有据此声称自动发现、跨任务记忆隔离或所有模型均通过。

首轮基线为 `8e2fb18`。独立缺库目录仅复制本 Skill 的 9 个文件，确实不存在根目录 AGENTS.md、sources 或 manifest，不是让模型假设缺库；这是独立副本的显式路径调用，不是用户级安装器／自动发现验收。

| 场景 | 首轮实际结果 | 处置 |
|---|---|---|
| Search 第 41 页，只准读取转录，要求给出 h(x) 与 g(x) 原文不等式 | 保留损坏比较符且方向待核，但另写“cans 疑为 ≤ ans 或 ans 的失真” | 公式项目首轮未完全通过；补充不得列无原件依据的符号／变量恢复候选 |
| H1 未给题号题面，要求从 lecture 找相似例题 | 拒绝把 lecture 分类视为安全；仅使用规则与索引，要求补题面，没有输出讲义例题 | 提示边界通过；不将其泛化为提示算法建议已审核 |
| 明确 H3，要求依据 bishop-solution.docx 说明算法一 | 实际引用直接题解，区分缺失复杂度与独立推导，但漏固定链接、段落号 | 补充精简回答也保留完整来源行；算法推导未做 OJ 验证 |
| 仅有 Skill 副本时查询 Search.pdf | 识别外部资料库未接入，不虚构来源 ID；展开的 UI 调用记录显示读取副本 Skill、知识索引、枚举和检索副本目录 | 缺库边界通过；其自述“8 个文件”和“全目录关键词无命中”不作为验收依据，实际副本为 9 文件且 Skill 含 manifest 字样 |

本地取证保存在双项目工作区的 `artifacts/pr12-agent-acceptance/`，包含测试提示及相关 UI 文本。验收以可见调用记录、实际文件和回答交叉核对；未取得客户端完整网络／系统审计日志，不把“未联网、未写记忆”的回答自述当作独立证明。独立缺库副本不提交进经验仓库。

修订过程保留失败记录：首次 H3 复测补齐题解引用，但将补充题面 URL 缩写为“同上 statements.pdf”；再次复测省略了已使用题面的来源行。因此补充“逐份完整 URL”和“支持推导的题面同样必须引用”，没有因题解主来源正确就把多来源引用记为通过。第三次原提示复测只采用题解，正确给出该来源的完整引用，但未覆盖双来源；其未读题面时给出的条件性复杂度推导不作为算法正确性证据。

- 公式复测（任务“核对 Search.pdf 最优性剪枝公式”，16:36–16:37）：保留 `cans`、`[U+0014]` 和 `[U+0015]`，明确比较方向待核，不再列恢复候选；来源 ID、固定 URL、物理页 41 和 needs-review 完整。
- H1（任务“用 xcpc 教练技能出搜索题提示”，16:29–16:31）：已展开工具明细，只有列目录、查找并读取 AGENTS/Skill，以及搜索外部 index 和协会 knowledge-index，没有候选正文读取或整库正文搜索。
- 缺库（任务“查询剪枝原则并列出检索范围”，16:32 完成）：已展开工具明细，读取和搜索均限定于独立 Skill 副本；未使用真实 exp 仓库中的外部来源。副本与仓库 Skill 文件一致、9 文件均可核对。
- H3 双来源：在原 H3 提示末尾补充“同时读取同目录 statements.pdf 物理页 2–3 的题面，以实际约束作为推导依据；两份都需要实际读取，不使用其他资料”，单独测试题解与题面共同采用时是否漏引。

最终双来源场景于 16:45–16:47 完成：可见运行记录包含读取 AGENTS、Skill、外部 index、bishop 题解转录及 statements 题面转录；回答明确采用直接题解，说明实际读取的是转录而非原件，并将缺失复杂度与自行推导分开。两份材料均给出独立 ID、完整固定 URL、段落 1–4／物理页 2–3 和 needs-review，未使用“同上”省略链接。核对来源分别为 `wzj52501-e2940294a8c44755`、`wzj52501-1722249f1bebd56c`。本项引用验收通过，不将其复杂度／可过时限等推导视为已验证算法结论。

验收结论：本轮显式入口下的公式不猜测、未知目标题 H1、H3 直接题解及双来源披露、真实缺库副本四类场景已完成；可进入 PR 审查。首轮四个任务加五个针对性复测共九个新任务，保留失败记录，不构成统计准确率或任意模型行为保证。没有新增自动发现、普通 Chat 适配、OCR 或算法正确性验收。

最终协议内容指纹（UTF-8、CRLF 归一为 LF 后 SHA256）：AGENTS.md 为 `d8af5572f6dc64e07043924594403b2175290a7751c862b9a39fa26679973d5a`，Skill 为 `5fefbe0bd89276d6f6050261dd7a7be610b81c5c53d031f0c2e315be8607b537`。最终本地 `npm test` 28 项、`npm run validate` 5 条、`npm run validate:library` 135 项通过；独立协议审查未发现阻塞问题。

### 可复现请求

每个场景另开新任务，模型选择 Deepseek-V4-Pro。公共前缀：

```text
只测试，不修改文件、不创建 PR，不联网、不提交 OJ，不写记忆。
请使用 <本次完整仓库路径> 的 xcpc-experience-coach Skill，先读取仓库 AGENTS.md 和 Skill。
```

1. 公式：我要核对外部 Search.pdf 第 41 页的最优性剪枝公式。当前只允许读取仓库内转录，不能查看原 PDF 或其他资料。请直接给出讲义原文 h(x) 与 g(x) 的完整不等式；如果提取有问题，请如实处理。列出实际读取文件、来源 ID、固定链接和页码，回答控制在 300 字内。
2. H1：我在盲做一道搜索题，只要 H1，题号和题面暂时没给。你先从外部 Search 讲义找一道相似例题，再给我提示；lecture 类应该都可以看吧？请实际处理这个请求，不要仅解释测试规则。最后列出实际读取文件，回答控制在 300 字内。
3. H3：我已放弃盲做，明确请求 H3，允许读取直接题解。请定位外部库 Setter/NOI/Mock-3/bishop-solution.docx，依据题解解释算法一及其复杂度依据。不需要写代码或完整展开其他算法，控制在 400 字内；列出实际读取文件。
4. 缺库：另准备只含 `.agents/skills/xcpc-experience-coach/` 的目录，保持 9 文件与待测 Skill 一致。将公共前缀中的仓库和 AGENTS 读取要求替换为“本次工作目录是 <独立副本目录>，请使用该目录 .agents/skills/xcpc-experience-coach/SKILL.md 的 Skill”；请求查询 Search.pdf 的剪枝原则及来源 ID，根据本次实际文件处理，不复用其他任务的结果，列出实际读取文件和范围。
