# 提取质量修复：首批原页对照

用户已要求开始解决乱码与公式缺失，沿用现有按需修复待办，不接入 RAG。

## 设计与范围

首批修复 Search.pdf 物理页 11、41、42 与 Dynamic-Programming.pdf 物理页 8。固定原件在本机归档中，先核对原件 SHA256，再用 Poppler 渲染对照。第 41 页含隐藏动画文字，旧提取错误地包含第 42 页才显示的估价函数段落，修订必须保持可见内容的物理页归属。

使用来源 ID、原路径、原件 SHA256、物理页码及原始提取段 SHA256 绑定修订；修订文本放入资料库 corrections.json，由离线导入器应用。原件或原始提取变化时失败，不静默覆盖。修訂仍保留 needs-review，并在输出页段标明局部视觉转录及原件定位。原有原始结果可以通过同一归档与禁用修订的导入调用复现。

这批不声称全库或整份讲义已复核。bishop-solution.docx 为嵌入图形公式，独立跟踪，未确认其准确文字前不填入猜测值。未修改 withheld 策略、上游许可与原件。

## 实施与验证

1. 写修订匹配、错哈希、重复定位、withheld 拒绝及未修页保留的 Python 回归；复现失败后实现最小导入扩展。
2. 写入已对照原页的四页转录及复核记录，离线重建，确认只改变目标正文与清单。
3. 更新 MCP 回归：已修第 41 页返回正确比较符和定位，未修第 43 页继续保留损坏符号；H1 仍无正文。
4. Python 测试、npm test、知识及来源校验、重复导入一致性、独立审查。修订记录与代码一起通过 PR 审核后才部署。

验收不包括自动 OCR 准确率、全库完整性或算法技术审核。
