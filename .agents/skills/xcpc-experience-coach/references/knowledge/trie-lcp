---
kind: algorithm
topics: trie, lcp, counting
evidence: single-case
authors: "@wfz"
status: active
updated: 2026/9/15
related:
---

# 用 Trie 前缀计数求不同选取数量的最长公共前缀

## 经验结论

当问题要求从 n 个字符串中选择 k 个，使其最长公共前缀尽可能长时，可以把“枚举字符串集合”转成“枚举 Trie 前缀节点”。

设节点 v 的深度为 depth[v]，经过它的字符串数量为 cnt[v]，则：

ans[k] = max{depth[v] | cnt[v] >= k}。

维护答案可以选择两种方式：

1. 插入过程中，每次 cnt[v] 增加到 t，就更新 ans[t]。
2. 建树完成后，按最终 cnt[v] 分桶记录最大深度，再做后缀最大值。

第一种方式已经覆盖所有计数阈值，不需要再做后缀最大值。

## 适用信号

- 需要选取若干字符串，优化它们的公共前缀长度。
- 需要求多个选取数量 k 对应的答案。
- 一个前缀能否被 k 个字符串共享，只取决于拥有该前缀的字符串数量。
- 字符串按输入项计数；重复字符串是否允许分别选取，需要核对题意。

## 常见 blocker 或失败症状

- 从字符串组合出发思考，难以避免枚举大量集合。
- 已经建好 Trie，但没有把节点深度、经过数量与答案对应起来。
- 每次插入经过节点时，重新更新整个 1..cnt[v] 区间，反复处理旧贡献。
- 混淆“逐次更新”和“最终分桶”的数组含义，误以为两者都必须做后缀最大值。

## 原因与原理

每个 Trie 节点代表一个确定的公共前缀。

如果 cnt[v] >= k，就能选出 k 个经过该节点的字符串，它们的公共前缀长度至少为 depth[v]。

反过来，若某 k 个字符串拥有长度 L 的公共前缀，Trie 中必然存在对应前缀节点，其深度为 L，经过数量至少为 k。

因此，枚举满足 cnt[v] >= k 的节点即可得到最优值。

## 验证方式

据投稿者自述，本经验来自一次已 AC 的 Trie/LCP 题目。当前草稿未附题目链接、提交记录或实际代码，整理者未独立核验 AC，也未运行测试。

上述答案公式与两种维护方式的等价性为整理时的独立推导，不作为额外实测记录。

提交前需补充题目来源，并确认实际 AC 实现采用哪种维护方式。

## 边界与反例

- 本经验针对公共前缀，不直接适用于最长公共子串或公共子序列。
- k 的范围为 1..n；若 k=1，应核对题目是否将单个字符串的 LCP 定义为自身长度。
- 默认 Trie 深度对应题目规定的字符长度单位。
- 若允许删除字符串，历史记录的最大值可能失效，不能直接沿用仅插入的维护方式。
- 若每次计数增加不止 1，逐次阈值更新不能直接简化为只更新新的计数值。
- 不存在非空公共前缀时答案为 0，可通过答案数组初始化为 0 处理。

## 来源

待补充：https://qoj.ac/contest/4071/problem/20027

已有材料：投稿者自述该题已 AC；暂未提供可独立核对的提交记录。

## 核心模型或不变量

方案 A：每次插入经过节点 v 时执行：

```cpp
++cnt[v];
ans[cnt[v]] = max(ans[cnt[v]], depth[v]);
```

节点最终计数为 c 时，它已经依次经过计数 1..c，并分别向这些答案贡献过自己的深度。

因此建树结束后：

ans[k] = max{depth[v] | cnt[v] >= k}。

方案 B：只按最终计数记录：

```cpp
best[cnt[v]] = max(best[cnt[v]], depth[v]);
```

此时 best[c] 表示最终经过数量恰好为 c 的节点的最大深度。随后：

```cpp
for (int k = n - 1; k >= 1; --k)
    best[k] = max(best[k], best[k + 1]);
```

才能得到经过数量至少为 k 的最大深度。

## 复杂度与约束

设字符串总长度为 S，Trie 节点数为 V，字符串数量为 n。

假设每次 Trie 转移为 O(1)：

- 方案 A：插入与贡献更新 O(S)，加上答案数组初始化和输出，总时间 O(S+n)。
- 方案 B：建树 O(S)，枚举节点 O(V)，后缀传播 O(n)，总时间 O(S+V+n)。
- V <= S+1，固定字符集下空间为 O(V+n)；若节点使用大小为字符集规模 sigma 的稠密转移表，空间为 O(V*sigma+n)。

需要区分两种区间枚举：

- 每次计数增长都扫描 1..当前计数：节点最终计数为 c 时累计 O(c²)，可能导致大量重复工作。
- 建树结束后，每个非根节点只扫描一次 1..最终计数：总工作量为 sum cnt[v] = S，同样是 O(S)，不能误判为必然超时。

具体时间和内存上限需结合原题约束核对。

## 实现风险

- cnt[v] 应统计拥有该前缀的字符串输入项数量，而非以该节点为结尾的字符串数量。
- 方案 A 必须在每次计数逐一增长时更新，不能只在结束时更新最终计数而省略传播。
- 方案 B 必须从大到小做后缀最大值。
- 方案 A 结束后再做后缀最大值不会改变正确结果，但属于冗余步骤。
- 每组数据需要正确清空节点计数、转移和答案数组。
- 节点容量应依据总字符数安排；不要只按字符串数量分配。

## 迁移识别

当一个状态的计数 c 能支持所有阈值 k<=c 时，先检查：

1. 计数是否从 0 开始逐一增长；
2. 状态贡献值是否固定；
3. 是否只需最终静态答案；
4. 是否存在删除或其他使旧贡献失效的操作。

若计数逐一增长且贡献不会失效，可以在新阈值首次达到时记录贡献。

若只关心最终答案，可以按最终计数分桶，再通过阈值包含关系传播。

判断优化是否必要时，应计算所有状态的累计工作量，而不是仅凭代码存在嵌套循环判断复杂度。
