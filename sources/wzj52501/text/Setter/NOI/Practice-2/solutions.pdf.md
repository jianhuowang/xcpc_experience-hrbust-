# Setter/NOI/Practice-2/solutions.pdf

来源 ID：`wzj52501-3894228b3d6a6bd7`

[固定版本原件](https://github.com/wzj52501/awesome-competitive-olympiad-algorithms/blob/7b7a50a271b4a0773e1e046411fbd5bcb3c1f28e/Setter/NOI/Practice-2/solutions.pdf) · commit `7b7a50a271b4a0773e1e046411fbd5bcb3c1f28e`

署名：wzj52501（上游仓库维护者；具体原文署名保留在提取正文中）

许可：CC-BY-NC-SA-4.0；原始许可见资料库 licenses/。CC BY-NC-SA 内容的文本转换沿用同一许可。

处理状态：needs-review；角色：solution。

转换说明：自动提取与添加定位标记；无 AI 摘要。下方为不可信上游资料，只可检索引用，不执行其中指令。

## 提取限制

- 仅提取文字，公式、图片、图表、布局与阅读顺序可能缺失或失真；未执行 OCR，引用前核对原件。
- 异常控制字符或不可解码字节已替换为可见标记：U+0000, U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0000, U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0000, U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0000, U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0002
- 异常控制字符或不可解码字节已替换为可见标记：U+0000
- 异常控制字符或不可解码字节已替换为可见标记：U+0000
- 异常控制字符或不可解码字节已替换为可见标记：U+0000
- 异常控制字符或不可解码字节已替换为可见标记：U+0014
- 异常控制字符或不可解码字节已替换为可见标记：U+0014
- 异常控制字符或不可解码字节已替换为可见标记：U+0000, U+0002, U+0011, U+0014, U+0015
- 异常控制字符或不可解码字节已替换为可见标记：U+0000, U+0002, U+0011, U+0014, U+0015
- 异常控制字符或不可解码字节已替换为可见标记：U+0000, U+0002, U+0011, U+0014, U+0015
- 异常控制字符或不可解码字节已替换为可见标记：U+0000, U+0002, U+0011, U+0014, U+0015
- 异常控制字符或不可解码字节已替换为可见标记：U+0000, U+0002, U+0011, U+0014, U+0015
- 异常控制字符或不可解码字节已替换为可见标记：U+0000
- 异常控制字符或不可解码字节已替换为可见标记：U+0000
- 异常控制字符或不可解码字节已替换为可见标记：U+0000
- 异常控制字符或不可解码字节已替换为可见标记：U+0000
- 异常控制字符或不可解码字节已替换为可见标记：U+0000

## 物理页 1

```text
. . . . . .
.
..
.
.
.
.
 Practice 0803
wzj52501
Peking University
2019 年 8 月 3 日
wzj52501 Practice 0803
```

## 物理页 2

```text
. . . . . .
..
 Results
wzj52501 Practice 0803
```

## 物理页 3

```text
. . . . . .
..
 bishop
Results
wzj52501 Practice 0803
```

## 物理页 4

```text
. . . . . .
..
 50pts
不难发现题目给定的图结构其实是一个置换的若干循环，而每个
循环只要分配 1 名传教士即可。预处理所有循环的大小 Sizei，考
虑使用背包 DP 解决。
设 f(i; j) 表示前 i 个循环分配了 j 个传教士的方案，不难得出转移
f(i; j) = ∑Size[i]
k=1 f(i [U+0000] 1; j [U+0000] k) [U+0002]
( Size[i]
k
)
。
虽然这个 DP 看起来很像 O(n3)，其实观察到这个式子本质上枚
举了前 i [U+0000] 1 个循环的所有元素与第 i 个循环的所有元素的所有配
对，而任意两个元素之间最多被配对一次，所以时间复杂度其实
是 O(n2) 的。
时间复杂度为 O(T [U+0002] n2)。
wzj52501 Practice 0803
```

## 物理页 5

```text
. . . . . .
..
 50pts
不难发现题目给定的图结构其实是一个置换的若干循环，而每个
循环只要分配 1 名传教士即可。预处理所有循环的大小 Sizei，考
虑使用背包 DP 解决。
设 f(i; j) 表示前 i 个循环分配了 j 个传教士的方案，不难得出转移
f(i; j) = ∑Size[i]
k=1 f(i [U+0000] 1; j [U+0000] k) [U+0002]
( Size[i]
k
)
。
虽然这个 DP 看起来很像 O(n3)，其实观察到这个式子本质上枚
举了前 i [U+0000] 1 个循环的所有元素与第 i 个循环的所有元素的所有配
对，而任意两个元素之间最多被配对一次，所以时间复杂度其实
是 O(n2) 的。
时间复杂度为 O(T [U+0002] n2)。
wzj52501 Practice 0803
```

## 物理页 6

```text
. . . . . .
..
 50pts
不难发现题目给定的图结构其实是一个置换的若干循环，而每个
循环只要分配 1 名传教士即可。预处理所有循环的大小 Sizei，考
虑使用背包 DP 解决。
设 f(i; j) 表示前 i 个循环分配了 j 个传教士的方案，不难得出转移
f(i; j) = ∑Size[i]
k=1 f(i [U+0000] 1; j [U+0000] k) [U+0002]
( Size[i]
k
)
。
虽然这个 DP 看起来很像 O(n3)，其实观察到这个式子本质上枚
举了前 i [U+0000] 1 个循环的所有元素与第 i 个循环的所有元素的所有配
对，而任意两个元素之间最多被配对一次，所以时间复杂度其实
是 O(n2) 的。
时间复杂度为 O(T [U+0002] n2)。
wzj52501 Practice 0803
```

## 物理页 7

```text
. . . . . .
..
 50pts
不难发现题目给定的图结构其实是一个置换的若干循环，而每个
循环只要分配 1 名传教士即可。预处理所有循环的大小 Sizei，考
虑使用背包 DP 解决。
设 f(i; j) 表示前 i 个循环分配了 j 个传教士的方案，不难得出转移
f(i; j) = ∑Size[i]
k=1 f(i [U+0000] 1; j [U+0000] k) [U+0002]
( Size[i]
k
)
。
虽然这个 DP 看起来很像 O(n3)，其实观察到这个式子本质上枚
举了前 i [U+0000] 1 个循环的所有元素与第 i 个循环的所有元素的所有配
对，而任意两个元素之间最多被配对一次，所以时间复杂度其实
是 O(n2) 的。
时间复杂度为 O(T [U+0002] n2)。
wzj52501 Practice 0803
```

## 物理页 8

```text
. . . . . .
..
 80pts
考虑每个循环的生成函数 A(x)，xi 的系数为该循环分配 i 个传教
士的方案数贡献。我们将所有的 A(x) 相乘得到 G(x)，则 G(x) 的
xi 系数即为分配 i 个传教士满足任意一个循环均有至少 1 个传教
士的方案数。
经过一些分析可以得到 A(i) = ∑Size[i]
k=1
( Size[i]
k
)
xk。
考虑不同的 Size 至多有 O(pn) 种，我们可以对于不同的 Size 使
用 NTT 计算 A(x) 的点值再快速幂合并起来。
时间复杂度为 O(T [U+0002] n1:5 log n)。
wzj52501 Practice 0803
```

## 物理页 9

```text
. . . . . .
..
 80pts
考虑每个循环的生成函数 A(x)，xi 的系数为该循环分配 i 个传教
士的方案数贡献。我们将所有的 A(x) 相乘得到 G(x)，则 G(x) 的
xi 系数即为分配 i 个传教士满足任意一个循环均有至少 1 个传教
士的方案数。
经过一些分析可以得到 A(i) = ∑Size[i]
k=1
( Size[i]
k
)
xk。
考虑不同的 Size 至多有 O(pn) 种，我们可以对于不同的 Size 使
用 NTT 计算 A(x) 的点值再快速幂合并起来。
时间复杂度为 O(T [U+0002] n1:5 log n)。
wzj52501 Practice 0803
```

## 物理页 10

```text
. . . . . .
..
 80pts
考虑每个循环的生成函数 A(x)，xi 的系数为该循环分配 i 个传教
士的方案数贡献。我们将所有的 A(x) 相乘得到 G(x)，则 G(x) 的
xi 系数即为分配 i 个传教士满足任意一个循环均有至少 1 个传教
士的方案数。
经过一些分析可以得到 A(i) = ∑Size[i]
k=1
( Size[i]
k
)
xk。
考虑不同的 Size 至多有 O(pn) 种，我们可以对于不同的 Size 使
用 NTT 计算 A(x) 的点值再快速幂合并起来。
时间复杂度为 O(T [U+0002] n1:5 log n)。
wzj52501 Practice 0803
```

## 物理页 11

```text
. . . . . .
..
 80pts
考虑每个循环的生成函数 A(x)，xi 的系数为该循环分配 i 个传教
士的方案数贡献。我们将所有的 A(x) 相乘得到 G(x)，则 G(x) 的
xi 系数即为分配 i 个传教士满足任意一个循环均有至少 1 个传教
士的方案数。
经过一些分析可以得到 A(i) = ∑Size[i]
k=1
( Size[i]
k
)
xk。
考虑不同的 Size 至多有 O(pn) 种，我们可以对于不同的 Size 使
用 NTT 计算 A(x) 的点值再快速幂合并起来。
时间复杂度为 O(T [U+0002] n1:5 log n)。
wzj52501 Practice 0803
```

## 物理页 12

```text
. . . . . .
..
 100pts
注意到我们在使用 NTT 合并两个大小为 Sa 与 Sb 的循环时，复
杂度其实是 O((Sa + Sb) log(Sa + Sb)) 的。
这与哈夫曼编码的过程其实很相似，我们使用哈夫曼编码的顺序
来合并，可以保证合并的复杂度最优。
另一种的合并方法是每轮将相邻的两个多项式合并，这样每轮的
复杂度均为 O(log n)，而至多进行 O(log n) 轮（因为每轮多项式
数量减半）
时间复杂度为 O(T [U+0002] n log2 n)。
wzj52501 Practice 0803
```

## 物理页 13

```text
. . . . . .
..
 100pts
注意到我们在使用 NTT 合并两个大小为 Sa 与 Sb 的循环时，复
杂度其实是 O((Sa + Sb) log(Sa + Sb)) 的。
这与哈夫曼编码的过程其实很相似，我们使用哈夫曼编码的顺序
来合并，可以保证合并的复杂度最优。
另一种的合并方法是每轮将相邻的两个多项式合并，这样每轮的
复杂度均为 O(log n)，而至多进行 O(log n) 轮（因为每轮多项式
数量减半）
时间复杂度为 O(T [U+0002] n log2 n)。
wzj52501 Practice 0803
```

## 物理页 14

```text
. . . . . .
..
 100pts
注意到我们在使用 NTT 合并两个大小为 Sa 与 Sb 的循环时，复
杂度其实是 O((Sa + Sb) log(Sa + Sb)) 的。
这与哈夫曼编码的过程其实很相似，我们使用哈夫曼编码的顺序
来合并，可以保证合并的复杂度最优。
另一种的合并方法是每轮将相邻的两个多项式合并，这样每轮的
复杂度均为 O(log n)，而至多进行 O(log n) 轮（因为每轮多项式
数量减半）
时间复杂度为 O(T [U+0002] n log2 n)。
wzj52501 Practice 0803
```

## 物理页 15

```text
. . . . . .
..
 100pts
注意到我们在使用 NTT 合并两个大小为 Sa 与 Sb 的循环时，复
杂度其实是 O((Sa + Sb) log(Sa + Sb)) 的。
这与哈夫曼编码的过程其实很相似，我们使用哈夫曼编码的顺序
来合并，可以保证合并的复杂度最优。
另一种的合并方法是每轮将相邻的两个多项式合并，这样每轮的
复杂度均为 O(log n)，而至多进行 O(log n) 轮（因为每轮多项式
数量减半）
时间复杂度为 O(T [U+0002] n log2 n)。
wzj52501 Practice 0803
```

## 物理页 16

```text
. . . . . .
..
 decoration
Results
wzj52501 Practice 0803
```

## 物理页 17

```text
. . . . . .
..
 30pts
对于列数较小的情况，这是一个经典的轮廓线 DP 问题。
时间复杂度为 O(n [U+0002] m [U+0002] 2m)。
wzj52501 Practice 0803
```

## 物理页 18

```text
. . . . . .
..
 30pts
对于列数较小的情况，这是一个经典的轮廓线 DP 问题。
时间复杂度为 O(n [U+0002] m [U+0002] 2m)。
wzj52501 Practice 0803
```

## 物理页 19

```text
. . . . . .
..
 50 - 65pts
如此大的数据范围让我们很自然联想到了矩阵快速幂。
设 f(i; S) 表示前 i [U+0000] 1 列已经填满，第 i 列的状态为 S 的方案数。
预处理 f(i; S) 到 f(i + 1; S2) 的转移矩阵，然后快速幂优化（需要
写十进制快速幂）
时间复杂度为 O(6m + 8m log r)。
wzj52501 Practice 0803
```

## 物理页 20

```text
. . . . . .
..
 50 - 65pts
如此大的数据范围让我们很自然联想到了矩阵快速幂。
设 f(i; S) 表示前 i [U+0000] 1 列已经填满，第 i 列的状态为 S 的方案数。
预处理 f(i; S) 到 f(i + 1; S2) 的转移矩阵，然后快速幂优化（需要
写十进制快速幂）
时间复杂度为 O(6m + 8m log r)。
wzj52501 Practice 0803
```

## 物理页 21

```text
. . . . . .
..
 50 - 65pts
如此大的数据范围让我们很自然联想到了矩阵快速幂。
设 f(i; S) 表示前 i [U+0000] 1 列已经填满，第 i 列的状态为 S 的方案数。
预处理 f(i; S) 到 f(i + 1; S2) 的转移矩阵，然后快速幂优化（需要
写十进制快速幂）
时间复杂度为 O(6m + 8m log r)。
wzj52501 Practice 0803
```

## 物理页 22

```text
. . . . . .
..
 80 - 90pts
实际上有用的状态数不足 2m，把从初始状态不可能到达与不可能
从此状态出发到达最终状态的状态去除，当 m = 8 时仅有 71 个
有用状态。
沿用上一个算法。
时间复杂度为 O(6m + k3 log r)，k [U+0014] 71。
wzj52501 Practice 0803
```

## 物理页 23

```text
. . . . . .
..
 80 - 90pts
实际上有用的状态数不足 2m，把从初始状态不可能到达与不可能
从此状态出发到达最终状态的状态去除，当 m = 8 时仅有 71 个
有用状态。
沿用上一个算法。
时间复杂度为 O(6m + k3 log r)，k [U+0014] 71。
wzj52501 Practice 0803
```

## 物理页 24

```text
. . . . . .
..
 100pts
矩阵乘法本身的复杂度很难得到优化（使用分治矩阵乘法优化并
不明显） ，我们考虑一个k [U+0002] k 的矩阵 A 的特征多项式
f(x) = j[U+0015]E [U+0000] Aj。
根据 Cayley-Hamilton 定理有 f(A) = 0 ，故有 Ar [U+0011] (Ar mod f(A))。
这样只需要求出 f(A) 后计算 Ar mod f(x)（暴力 O(k2 log r) 或者
多项式除法优化 O(k log k log r)）就可以在 O(k4) 的时间内求出来
Ak。
考虑到直接求元素是多项式的矩阵的行列式很麻烦，我们可以先
随便代入 k + 1 个不同的求出的 k + 1 个点值，最后通过拉格朗日
插值还原出 f(x)。
时间复杂度为 O(6m + k2 log r + k4)，k [U+0014] 71。
wzj52501 Practice 0803
```

## 物理页 25

```text
. . . . . .
..
 100pts
矩阵乘法本身的复杂度很难得到优化（使用分治矩阵乘法优化并
不明显） ，我们考虑一个k [U+0002] k 的矩阵 A 的特征多项式
f(x) = j[U+0015]E [U+0000] Aj。
根据 Cayley-Hamilton 定理有 f(A) = 0 ，故有 Ar [U+0011] (Ar mod f(A))。
这样只需要求出 f(A) 后计算 Ar mod f(x)（暴力 O(k2 log r) 或者
多项式除法优化 O(k log k log r)）就可以在 O(k4) 的时间内求出来
Ak。
考虑到直接求元素是多项式的矩阵的行列式很麻烦，我们可以先
随便代入 k + 1 个不同的求出的 k + 1 个点值，最后通过拉格朗日
插值还原出 f(x)。
时间复杂度为 O(6m + k2 log r + k4)，k [U+0014] 71。
wzj52501 Practice 0803
```

## 物理页 26

```text
. . . . . .
..
 100pts
矩阵乘法本身的复杂度很难得到优化（使用分治矩阵乘法优化并
不明显） ，我们考虑一个k [U+0002] k 的矩阵 A 的特征多项式
f(x) = j[U+0015]E [U+0000] Aj。
根据 Cayley-Hamilton 定理有 f(A) = 0 ，故有 Ar [U+0011] (Ar mod f(A))。
这样只需要求出 f(A) 后计算 Ar mod f(x)（暴力 O(k2 log r) 或者
多项式除法优化 O(k log k log r)）就可以在 O(k4) 的时间内求出来
Ak。
考虑到直接求元素是多项式的矩阵的行列式很麻烦，我们可以先
随便代入 k + 1 个不同的求出的 k + 1 个点值，最后通过拉格朗日
插值还原出 f(x)。
时间复杂度为 O(6m + k2 log r + k4)，k [U+0014] 71。
wzj52501 Practice 0803
```

## 物理页 27

```text
. . . . . .
..
 100pts
矩阵乘法本身的复杂度很难得到优化（使用分治矩阵乘法优化并
不明显） ，我们考虑一个k [U+0002] k 的矩阵 A 的特征多项式
f(x) = j[U+0015]E [U+0000] Aj。
根据 Cayley-Hamilton 定理有 f(A) = 0 ，故有 Ar [U+0011] (Ar mod f(A))。
这样只需要求出 f(A) 后计算 Ar mod f(x)（暴力 O(k2 log r) 或者
多项式除法优化 O(k log k log r)）就可以在 O(k4) 的时间内求出来
Ak。
考虑到直接求元素是多项式的矩阵的行列式很麻烦，我们可以先
随便代入 k + 1 个不同的求出的 k + 1 个点值，最后通过拉格朗日
插值还原出 f(x)。
时间复杂度为 O(6m + k2 log r + k4)，k [U+0014] 71。
wzj52501 Practice 0803
```

## 物理页 28

```text
. . . . . .
..
 100pts
矩阵乘法本身的复杂度很难得到优化（使用分治矩阵乘法优化并
不明显） ，我们考虑一个k [U+0002] k 的矩阵 A 的特征多项式
f(x) = j[U+0015]E [U+0000] Aj。
根据 Cayley-Hamilton 定理有 f(A) = 0 ，故有 Ar [U+0011] (Ar mod f(A))。
这样只需要求出 f(A) 后计算 Ar mod f(x)（暴力 O(k2 log r) 或者
多项式除法优化 O(k log k log r)）就可以在 O(k4) 的时间内求出来
Ak。
考虑到直接求元素是多项式的矩阵的行列式很麻烦，我们可以先
随便代入 k + 1 个不同的求出的 k + 1 个点值，最后通过拉格朗日
插值还原出 f(x)。
时间复杂度为 O(6m + k2 log r + k4)，k [U+0014] 71。
wzj52501 Practice 0803
```

## 物理页 29

```text
. . . . . .
..
 conference
Results
wzj52501 Practice 0803
```

## 物理页 30

```text
. . . . . .
..
 30pts
对于每个询问，把路径上所有点的信息取出，问题转化为数轴上
有若干个点，每个位置有 Ai 个点，你要在其中选择一个位置作为
集合点，让所有点到该位置集合的距离之和最小。
显然应该取这若干个点的中位数位置。
时间复杂度为 O(nq)。
wzj52501 Practice 0803
```

## 物理页 31

```text
. . . . . .
..
 30pts
对于每个询问，把路径上所有点的信息取出，问题转化为数轴上
有若干个点，每个位置有 Ai 个点，你要在其中选择一个位置作为
集合点，让所有点到该位置集合的距离之和最小。
显然应该取这若干个点的中位数位置。
时间复杂度为 O(nq)。
wzj52501 Practice 0803
```

## 物理页 32

```text
. . . . . .
..
 30pts
对于每个询问，把路径上所有点的信息取出，问题转化为数轴上
有若干个点，每个位置有 Ai 个点，你要在其中选择一个位置作为
集合点，让所有点到该位置集合的距离之和最小。
显然应该取这若干个点的中位数位置。
时间复杂度为 O(nq)。
wzj52501 Practice 0803
```

## 物理页 33

```text
. . . . . .
..
 100pts
考虑用数据结构优化这一过程，我们需要解决的问题有 3 个：
更改一个节点的权值。
查询一段路径 u ! v 上带权的中位数节点。
查询一段路径 u ! v 上所有点到某一点 x 带权距离之和。
对于问题 1、2，我们可以考虑在 ST 表上倍增，使用另一个数据
结构带修改维护路径权值和就可以在 O(n log n) [U+0000] O(log2 n) 的时
间内解决。也可以使用 LCT 维护树结构，在 splay 树上查找答案，
这样的复杂度是 O(n) [U+0000] O(log n)。
wzj52501 Practice 0803
```

## 物理页 34

```text
. . . . . .
..
 100pts
考虑用数据结构优化这一过程，我们需要解决的问题有 3 个：
更改一个节点的权值。
查询一段路径 u ! v 上带权的中位数节点。
查询一段路径 u ! v 上所有点到某一点 x 带权距离之和。
对于问题 1、2，我们可以考虑在 ST 表上倍增，使用另一个数据
结构带修改维护路径权值和就可以在 O(n log n) [U+0000] O(log2 n) 的时
间内解决。也可以使用 LCT 维护树结构，在 splay 树上查找答案，
这样的复杂度是 O(n) [U+0000] O(log n)。
wzj52501 Practice 0803
```

## 物理页 35

```text
. . . . . .
..
 100pts
考虑用数据结构优化这一过程，我们需要解决的问题有 3 个：
更改一个节点的权值。
查询一段路径 u ! v 上带权的中位数节点。
查询一段路径 u ! v 上所有点到某一点 x 带权距离之和。
对于问题 1、2，我们可以考虑在 ST 表上倍增，使用另一个数据
结构带修改维护路径权值和就可以在 O(n log n) [U+0000] O(log2 n) 的时
间内解决。也可以使用 LCT 维护树结构，在 splay 树上查找答案，
这样的复杂度是 O(n) [U+0000] O(log n)。
wzj52501 Practice 0803
```

## 物理页 36

```text
. . . . . .
..
 cont’d
对于问题 1、3，把路径拆成 3 段：u ! x，x ! lca，lca ! v，每
一段都可以使用 DFS 序＋树状数组或树链剖分＋线段树在
O(n log n) [U+0000] O(log n) 或 O(n log n) [U+0000] O(log2 n) 的时间内解决。
时间复杂度：O(n log n) [U+0000] O(log2 n) 或 O(n log n) [U+0000] O(log n)。
wzj52501 Practice 0803
```

## 物理页 37

```text
. . . . . .
..
 cont’d
对于问题 1、3，把路径拆成 3 段：u ! x，x ! lca，lca ! v，每
一段都可以使用 DFS 序＋树状数组或树链剖分＋线段树在
O(n log n) [U+0000] O(log n) 或 O(n log n) [U+0000] O(log2 n) 的时间内解决。
时间复杂度：O(n log n) [U+0000] O(log2 n) 或 O(n log n) [U+0000] O(log n)。
wzj52501 Practice 0803
```

## 物理页 38

```text
. . . . . .
..
 THX
Q & A
wzj52501 Practice 0803
```
